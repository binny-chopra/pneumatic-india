# No environment variables are required for this project.
#
# The contact form uses FormSubmit (https://formsubmit.co), which needs no keys
# or accounts — just a one-time activation click in the business inbox after the
# first submission. To change the recipient, edit `email` in src/data/company.js.
